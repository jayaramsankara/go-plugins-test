package main


func makeRange(min, max int) []int {
    a := make([]int, max-min+1)
    for i := range a {
        a[i] = min + i
    }
    return a
}

func Counter () []int {
	return makeRange(100, 110)
}